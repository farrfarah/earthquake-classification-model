# earthquake-classification-model
Developed a machine learning model to classify earthquake events in Indonesia based on depth (shallow, intermediate, deep) by comparing Random Forest and AdaBoost Random Forest to improve classification performance on imbalanced data.
